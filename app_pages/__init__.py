"""Streamlit ``st.navigation`` page modules.

Per the multipage-apps Streamlit convention, this directory must be named
``app_pages/`` rather than ``pages/`` to avoid colliding with Streamlit's
legacy auto-discovery behavior.
"""
